Run command:

```bash
CUDA_VISIBLE_DEVICES=0,1,2,3 python train_from_init.py --session_name train_from_init_10 -n 1 -g 4 -nr 0 --max_epoches 60  --lr 0.04 --cls_step 0.5 --seg_lr_scale 0.1
```

(Depends on your gpu amount and memory)